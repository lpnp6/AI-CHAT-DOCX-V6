[project]
name = "docx-edit-mvp"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["lxml>=5"]

[project.scripts]
docx-edit-mvp = "docx_mvp.__main__:main"

[tool.setuptools]
packages = ["docx_mvp"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
